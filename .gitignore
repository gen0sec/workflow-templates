# Generated by scripts/build-manifest. Tracked in git so a reviewer
# can see what's being published in the PR diff, but the value the
# build action computes is the one that ships — we don't trust the
# checked-in copy.
# (Comment kept; manifest.json IS committed for visibility.)
/manifest.json.tmp

# Go test/build artifacts
/scripts/**/bin/
/scripts/**/*.test
